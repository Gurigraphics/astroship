---
import { getCollection } from "astro:content";
import { Picture } from "@astrojs/image/components";
import Layout from "@layouts/Layout.astro";
import Container from "@components/container.astro";
import Sectionhead from "@components/sectionhead.astro";

// Filter team entries with 'draft: false' & date before current date
const publishedTeamMembers = await getCollection("team", ({ data }) => {
  return !data.draft && data.publishDate < new Date();
});
---

<Layout title="About">
  <Container>
    <Sectionhead>
      <Fragment slot="title">Agenda</Fragment>
      <Fragment slot="desc">Gestão de reuniões </Fragment>
    </Sectionhead>

    <div class="flex flex-col gap-3 mx-auto max-w-4xl mt-16">
      <h2 class="font-bold text-3xl text-gray-800">
        Sobre nós
      </h2>
      <p class="text-lg leading-relaxed text-slate-500">
        Ajudamos advogados autônomos na gestão de suas reuniões. </br></br>
      Somos uma pequena empresa SaaS autofinanciada. 
      Nossos clientes tomam as decisões que nos ajudam a moldar o futuro de nossa plataforma de agendamento de consultas.</br></br>
      Lutamos contra todo o atrito que impede as pessoas de colocar energia em agendar reuniões.
      </p>
    </div>

    <div class="grid md:grid-cols-3 gap-10 mx-auto max-w-4xl mt-12">
     
    </div>
  </Container>
</Layout>
